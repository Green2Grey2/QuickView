# AppImage packaging (stub)

There are multiple ways to ship an AppImage for a GTK4/libadwaita app:

- Use a Docker-based build that bundles required libraries.
- Or prefer Flatpak on GNOME/Wayland systems.

This folder is intentionally a stub; fill it in once the MVP stabilizes.

